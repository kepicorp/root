// Alliance setup. Phase 4 fills this in.
//
// Per Root rules:
//   • Draw 3 cards for hand.
//   • Draw 3 cards for supporters deck (hidden).
//   • No starting board presence — bases and sympathy come from play.

import type { GameState } from '../../types';

export function setupAlliance(state: GameState): GameState {
  return state; // Phase 4 will implement.
}
